package ${package.Service};

import org.springframework.stereotype.Service;
import ${package.Entity}.${entity};
import ${superServiceClassPackage};
import com.ithinkdt.web.common.service.BaseService;

/**
 * <p>
 * ${table.comment!} 服务类 自定义
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
@Service
public interface ${table.serviceName} extends BaseService {

}
